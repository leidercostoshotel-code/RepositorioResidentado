{
  "name": "residentado-tutor-ia",
  "description": "Proxy de Claude para el banco de preguntas de Residentado Medico",
  "main": "index.js",
  "private": true,
  "engines": {
    "node": "22"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-functions": "^6.0.1"
  }
}
