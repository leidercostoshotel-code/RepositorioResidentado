{
  "name": "residentado-tutor-ia",
  "description": "Funciones de Residentado Medico: entrega del banco con licencia y proxy de Claude",
  "main": "index.js",
  "private": true,
  "engines": {
    "node": "22"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-functions": "^6.0.1",
    "firebase-admin": "^13.0.0"
  }
}
